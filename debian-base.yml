---
  - hosts: all
    become: True
    roles: 
      - debian-base
    vars:
      debian_user: freddy
      debian_version: bookworm
      debian_host: 10.172.0.9
      debian_private_key_file: ~/.ssh/fcch-ssh
      debian_port: 22
      ansible_python_interpreter: /usr/bin/python3

# Debian Version Name: bookworm
